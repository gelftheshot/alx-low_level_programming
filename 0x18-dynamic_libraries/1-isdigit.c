#include "main.h"
/**
* _isdigit - chechk if the givern numbr is digit
* @c: is comparing variable
* Return: 1 if digit 0 else
*/
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
{
	return (1);
}
	else
{
	return (0);
}

}

