#include "main.h"

/**
 * print_line - prints stight line
 * @n: number of tims the _ prited
 *
 * Return: stright line`.
 */
void print_line(int n)
{
	int i = 0;

	for (; i < n; i++)
{
	if (n <= 0)
{
	break;
}
	_putchar(95);
}
	_putchar('\n');

}
